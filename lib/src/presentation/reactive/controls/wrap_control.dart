import 'package:flutter/widgets.dart';
import 'package:reactive_forms_json_scheme/src/_src.dart';

class WrapControl extends StatelessWidget {
  const WrapControl({required this.uiSchema, required this.schema, super.key});

  final UISchemaElement uiSchema;
  final JsonSchema4 schema;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
